# ────────────────────────────────────────────────────────────────────
#  Civil Auto Workspace — Runtime Dependencies
#  pip install -r requirements.txt
# ────────────────────────────────────────────────────────────────────

# ── UI 层 (PySide6 + Win11 Fluent) ─────────────────────────────────
PySide6>=6.7,<7.0
PySide6-Fluent-Widgets>=1.7.0       # qfluentwidgets：原生 Win11 Fluent 控件

# ── 配置 / 校验 ────────────────────────────────────────────────────
PyYAML>=6.0                          # config.yaml 解析
pydantic>=2.5                        # 类型校验、路径检查、枚举

# ── Office 自动化（Windows） ───────────────────────────────────────
pywin32>=306                         # Word / Excel / WPS COM
python-docx==1.2.0                   # 离线 .docx 读写
openpyxl==3.1.5                      # 保格式 Excel 读写

# ── 数据科学 ───────────────────────────────────────────────────────
pandas>=2.2
numpy>=1.26
matplotlib>=3.9                      # 嵌入 FigureCanvasQTAgg

# ── PDF / 图像 ─────────────────────────────────────────────────────
pymupdf>=1.24                        # PDF→PNG / PDF 读写
pypdf>=4.0                           # PDF 排序合并
pillow>=10.4                         # 图片处理
handright==8.2.0                     # 仿生手写体

# ── 通用工具 ───────────────────────────────────────────────────────
lxml>=5.0
python-dateutil>=2.9
